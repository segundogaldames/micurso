{% extends "layout/default/template.twig" %}
{% block title %} {{title}} {% endblock %}

{% block content %}

<div class="app-content">
    <!--begin::Container-->
    <div class="container-fluid">
        <div class="card">
            <div class="card-header">
                <h1 class="fs-3">
                    {{subject}}
                    {% if action == 'index' %}
                        <a href="{{BASE}}{{route_create}}" class="btn btn-outline-secondary"> {{button_create}} </a>
                    {% endif %}
                </h1>
                
            </div>
            <div class="card-body">
                {% include "partials/_messages_admin.twig" %}
                {% if users is not empty %}
                    <table class="table table-hover" id="myTable">
                        <thead>
                            <tr>
                                <th class="col-1">Id</th>
                                <th class="col-3">Nombre</th>
                                <th class="col-3">Email</th>
                                <th class="col-2">Rol</th>
                                <th class="col-2">Activo</th>
                                <th class="col-1">Actividades</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for model in users %}
                                <tr>
                                    <td> {{model.id}} </td>
                                    <td> {{model.name}} </td>
                                    <td> {{model.email}} </td>
                                    <td> {{model.role.name}} </td>
                                    <td> 
                                        {% if model.active == 1 %}
                                            <span class="badge text-bg-success">Si</span>
                                        {% else %}
                                            <span class="badge text-bg-danger">No</span> 
                                        {% endif %}
                                     </td>
                                    <td>
                                        <a href="{{BASE}}users/show/{{model.id}}" class="btn btn-success btn-sm"><i class="bi bi-eye"></i></a>
                                        <a href="{{BASE}}users/edit/{{model.id}}" class="btn btn-warning btn-sm"><i class="bi bi-pencil-square"></i></a>
                                    </td>
                                </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                {% else %}
                    <p class="text-secondary fs-4">No hay usuarios disponibles</p>
                {% endif %}
                
            </div>
        </div>
    </div>
    <!--end::Container-->
</div>

{% endblock %}
